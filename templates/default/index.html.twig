{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>Bienvenue sur Todo List, l'application vous permettant de gérer l'ensemble de vos tâches sans effort !</h1>
{% endblock %}

{% block body %}

    {% if app.user %}

        <div class="row">
            {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                <a href="{{ path('task_create') }}" class="btn btn-success mr-2">Créer une nouvelle tâche</a>
            {% endif %}
            <a href="{{ path('task_list') }}" class="btn btn-info mr-2">Consulter la liste des tâches à faire</a>
            <a href="" class="btn btn-secondary">Consulter la liste des tâches terminées</a>
        </div>

    {% else %}

        <p>Todo List vous aide à vous concentrer, aussi bien dans vos activités personnelles que professionnelles.</p>
        <a href="{{ path('app_register') }}" class="btn btn-primary">Commencer</a>

    {% endif %}

    
{% endblock %}